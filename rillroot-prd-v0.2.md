# Rillroot PRD v0.2

| 항목          | 내용                                                                       |
| ------------- | -------------------------------------------------------------------------- |
| 제품          | Rillroot (`rillroot.com`)                                                  |
| 상태          | 2차 제품 요구사항 초안                                                     |
| 시장          | 글로벌, 웹 기반                                                            |
| 제품 형태     | Web-first 공개 열람형·창작자 중심 게시 플랫폼                              |
| 인증          | Supabase Auth 기반 Google·이메일 로그인                                    |
| 전달 구조     | 웹 클라이언트와 API 서버를 포함한 모노레포; 이후 React Native 앱 확장 고려 |
| MVP 지원 언어 | 영어 `en`, 한국어 `ko`, 일본어 `ja`, 스페인어 `es`                         |

## 1. 제품 요약

Rillroot는 누구나 로그인 없이 읽을 수 있고, 로그인한 창작자는 글을 발행하며 선택적으로 자신이 가진 외부 채널에도 함께 배포할 수 있는 글로벌 게시 플랫폼이다.

배포 대상은 두 종류다. **남의 피드에 닿는 소셜 채널**과 **창작자가 소유한 웹사이트**이며, 둘은 도달과 소유라는 다른 목적을 갖는다.

게시글은 창작자가 나눈 조각으로 이루어진다. 조각이 하나면 단일 게시글이고 여럿이면 연속 게시이며, 이는 외부 소셜 채널의 연속 게시와 같은 구조다. 연속 게시를 지원하지 않는 채널에서는 조각을 하나로 합친다.

핵심은 **글을 쓰고, 발견되고, 누적되는 창작자 플랫폼**이다. 초기 목표는 수익화가 아니라 양질의 글과 반복 방문을 만드는 것이다.

## 2. 문제와 기회

새 창작자는 처음부터 독자를 모아야 하고, 같은 글을 여러 소셜 채널에 다시 올리는 데 반복 작업이 든다. 또한 작은 창작자는 플랫폼 성장에 기여해도 보상과 소유의 체감을 얻기 어렵다.

Rillroot는 다음의 선순환을 만들고자 한다.

`쉬운 발행 → 선택적 동시 배포 → 창작자 활동 유지 → 공개 글 축적 → 독자 발견·재방문 → 장기적 보상 기회`

## 3. 제품 원칙

1. **읽기는 열고, 창작은 가볍게 전환한다.** 비회원은 모든 공개 글을 읽을 수 있다. 글 작성과 외부 동시 배포는 로그인한 회원만 할 수 있다. 로그인은 로그인 버튼을 누르거나 작성·저장(북마크)·팔로우 등 회원 행동을 시도할 때 연다.
2. **카테고리보다 글을 먼저 둔다.** 초기 사이트 IA에 고정 카테고리 탭을 두지 않는다.
3. **배포는 창작자 편의 기능이다.** 외부 동시 배포는 플랫폼 성장의 자동 해법으로 과장하지 않는다. 같은 글을 외부에 올리는 것만으로는 Rillroot 유입이 보장되지 않는다.
4. **보상은 신뢰가 생긴 뒤에 도입한다.** 조회·클릭당 즉시 현금 보상은 어뷰징을 부른다. 광고 수익 정산은 검증된 지표와 정책이 갖춰진 뒤에만 검토한다.
5. **AI는 현재 범위 밖이다.** AI 글 생성은 콘텐츠 품질·정체성에 대한 명확한 가설이 생길 때까지 보류한다.

## 4. 대상 사용자

### 독자

- 관심 있는 주제를 가볍게 탐색하고, 로그인 없이 글을 소비하는 글로벌 사용자
- 외부 Bluesky, Mastodon, LinkedIn 등에서 창작자를 발견하는 사용자

### 창작자

- 개인적인 관점, 지식, 기록을 꾸준히 발행하는 개인
- 같은 글을 여러 채널에 반복 게시하는 부담을 줄이고 싶은 사용자
- 향후 자신의 기여가 명확히 보이고 보상으로도 이어지길 기대하는 사용자

## 5. 핵심 사용자 흐름

### 5.1 독자 → 회원 전환

1. 비회원이 홈, 검색/탐색, 외부 공유 링크로 공개 글에 도착한다.
2. 글을 제한 없이 읽는다.
3. 상단바의 로그인 버튼을 누르거나, 작성·저장(북마크)·팔로우 등 회원 행동을 시도하면 로그인을 연다.
4. Google 계정으로 인증하거나, 이메일을 입력해 그 주소로 받은 인증코드로 인증한다.
5. 처음 인증한 사용자는 회원으로 등록되는 동시에 로그인되고, 기존 회원은 바로 로그인된다.

### 5.2 창작 및 발행

1. 로그인한 창작자가 새 게시글을 작성한다. 게시글은 하나 이상의 조각으로 이루어지며, 나누는 지점은 창작자가 정한다.
2. 태그/토픽과 공개 여부를 설정한다.
3. 같은 화면에서 Bluesky·Mastodon·LinkedIn·WordPress 중 함께 배포할 채널을 선택한다. 연동되지 않은 채널은 그 자리에서 연동한다. 채널이 요구하는 제목이나 설정은 그 채널을 켤 때만 나타난다.
4. Rillroot에 먼저 발행하고, 선택된 채널에는 각 채널의 한도와 연속 게시 지원 여부에 맞춘 형태로 배포한다.
5. 각 채널의 진행·성공·실패 상태와 외부 게시물 URL을 창작자에게 보여 준다.

## 6. MVP 범위

### 포함

- 공개 홈과 공개 포스트 페이지
- Supabase Auth 로그인: Google, 이메일 인증코드
- 조각 단위 게시글 작성, 임시 저장, 발행, 수정, 삭제
- 작성자 프로필과 작성글 목록
- 태그/토픽 메타데이터 및 기본 탐색
- Bluesky, Mastodon, LinkedIn, WordPress 직접 연동 및 선택적 동시 배포
- 기본 발행·조회·외부 배포 이벤트 분석
- 영어·한국어·일본어·스페인어 UI 및 언어별 콘텐츠 메타데이터
- 반응형 다크/라이트 UI

### 제외

- React Native 네이티브 앱의 실제 출시
- 고정된 두 개의 사이트 카테고리와 카테고리별 IA
- 조회·클릭 기반 현금 자동 보상
- 광고 네트워크와 수익 분배
- 공개 Open API·MCP 서버
- AI 글 생성 또는 AI 에이전트 발행
- MVP 채널 외 소셜 채널의 지원
- Google 외 소셜 로그인

### 6.1 제품 전달 구조와 앱 확장 원칙

MVP는 **웹을 먼저 출시**한다. 이는 웹만을 전제로 도메인 로직을 설계한다는 뜻이 아니다. 첫 구현부터 웹 클라이언트와 API 서버를 하나의 모노레포로 운영하고, 이후 React Native 앱이 같은 API·공통 타입·디자인 원칙을 소비할 수 있게 한다.

초기 패키지 구조는 최소로 시작한다. `~/workspace/krill-protocol`의 `packages/shared` 구성 방식을 참조해 공통 기반을 두고, `~/workspace/rillroot`의 기존 `packages` 구조는 그대로 승계한다. 명확한 소유 경계나 두 개 이상의 소비자가 생기기 전에는 새 패키지를 만들지 않는다.

| 경계              | MVP 역할                                                                   | React Native 확장 시                                     |
| ----------------- | -------------------------------------------------------------------------- | -------------------------------------------------------- |
| `apps/web`        | 공개 읽기, 인증 진입, 에디터, 창작자 관리 UI 및 `next-intl` 기반 웹 현지화 | 웹 전용 UI로 유지                                        |
| `apps/api`        | 인증 검증, 콘텐츠·프로필, 배포 작업, 분석 이벤트의 단일 서버 경계          | 모바일 앱도 동일 API 사용                                |
| `packages/shared` | 상수, 타입, 유틸리티, 열거형, 언어 설정·번역 리소스 및 필요한 공통 스키마  | 앱·웹·API가 공유하는 프레임워크 독립 기반                |
| `packages/ui`     | 디자인 토큰, UI 규칙, 웹 공통 UI의 원천                                    | 토큰·상태 규칙을 공유하고, 네이티브 컴포넌트는 별도 구현 |
| 기존 `packages/*` | 현재 Rillroot의 패키지 구조를 유지                                         | 실제 필요가 확인될 때만 확장                             |
| `apps/mobile`     | MVP에서는 만들지 않음                                                      | React Native 앱을 추가할 위치                            |

아래 원칙을 MVP부터 지킨다.

- 비즈니스 규칙과 API 계약을 웹 컴포넌트 안에 묻지 않는다. 요청·응답 타입과 검증 스키마는 `packages/shared`에서 공유할 필요가 생긴 경우에만 둔다. 별도의 `packages/api-contract`는 초기에는 만들지 않는다.
- 웹과 모바일은 **`packages/ui`의 토큰과 UX 원칙을 공유**하되, React DOM 컴포넌트를 React Native에서 억지로 재사용하지 않는다. 플랫폼별 컴포넌트는 분리하고 색상·간격·상태 규칙을 공유한다.
- `next-intl`의 Next.js 라우팅·서버 렌더링 연결은 `apps/web`에 한정한다. 메시지 카탈로그와 locale 규칙은 `packages/shared`에 두어, 추후 모바일에서 별도의 React Native 어댑터로 재사용한다.
- 외부 채널 배포, 토큰 검증, 콘텐츠 권한, 분석 기록은 클라이언트가 아닌 API 서버가 책임진다.
- 포스트 URL과 작성자 URL은 앱 딥링크/유니버설 링크로 발전할 수 있도록 안정적인 식별자와 공개 URL을 유지한다.
- 모바일 반응형 웹은 MVP에 포함하지만, 이는 네이티브 앱 대체가 아니라 읽기·인증·작성 경험의 최소 품질 기준이다.

## 7. 콘텐츠 모델

초기에는 여행·음식·경제·테크처럼 사이트 전반의 고정 분류를 앞세우지 않는다. 대신 글에는 작성자가 붙이는 토픽을 둔다.

- 토픽/태그: 글의 탐색성과 검색 품질을 위한 공개 메타데이터
- 향후 판단: 실제 읽기·작성·재방문 데이터를 바탕으로 특정 주제의 허브, 큐레이션 또는 카테고리 UI를 도입한다.

초기 두 개의 주제는 제품 IA가 아니라, 필요하다면 시드 창작자 모집과 초기 편집 큐레이션을 위한 운영 단위로만 사용한다.

### 7.1 다국어 모델

MVP 지원 언어는 영어(`en`), 한국어(`ko`), 일본어(`ja`), 스페인어(`es`)다. 영어를 기본 폴백 언어로 두되, 브라우저 설정과 사용자의 선택을 우선한다.

웹의 UI 현지화는 `next-intl`로 구현한다. locale 기반 라우팅, 서버 렌더링, 날짜·숫자 포맷팅 등 Next.js에 결합된 기능은 `apps/web`이 담당한다. 번역 메시지와 지원 언어 목록은 `packages/shared/i18n`에 둔다.

| 데이터                        | 의미                                  | MVP 원칙                                            |
| ----------------------------- | ------------------------------------- | --------------------------------------------------- |
| `ui_locale`                   | 인터페이스에 사용하는 언어            | `en`, `ko`, `ja`, `es` 중 하나를 사용자가 변경 가능 |
| `content_language`            | 게시글의 작성 언어                    | 게시글마다 하나의 언어를 명시                       |
| `preferred_content_languages` | 피드에서 우선적으로 보고 싶은 글 언어 | 설정에서 복수 선택 가능                             |

- UI 현지화와 글 번역은 별개의 기능이다. MVP는 원문을 우선하며, 자동 번역·AI 번역은 포함하지 않는다.
- 인증 메일은 MVP에서 영어로 발송한다.
- 글의 언어는 발행 시 저장하고, 검색·추천·외부 배포에 활용한다.
- 외부 채널 동시 배포 시 작성 언어와 문체를 유지한다.
- 스페인어는 초기에 중립적인 `es` 카피로 운영하고, 트래픽·운영 수요가 확인되면 `es-ES`, `es-MX`처럼 지역 변형을 확장한다.
- 언어 태그와 API 계약은 BCP 47 형식을 전제로 설계해 이후 지역화와 모바일 앱을 수용한다.
- React Native 앱은 `next-intl`을 직접 이식하지 않는다. 동일한 메시지 카탈로그·locale 규칙을 사용하되, 앱 착수 시 React Native 환경에 맞는 i18n 어댑터와 기기 언어·저장소 연동을 확정한다.

## 8. 외부 배포 정책

### MVP 배포 채널

| 채널      | 성격        | 목적                    | 연속 게시 | 원칙                                     |
| --------- | ----------- | ----------------------- | --------- | ---------------------------------------- |
| Bluesky   | 소셜 채널   | 공개 소셜·초기 커뮤니티 | 지원      | 창작자가 명시적으로 선택한 경우에만 게시 |
| Mastodon  | 소셜 채널   | 텍스트 중심의 공개 대화 | 지원      | 창작자가 명시적으로 선택한 경우에만 게시 |
| LinkedIn  | 소셜 채널   | 전문성·커리어 맥락      | 없음      | 창작자가 명시적으로 선택한 경우에만 게시 |
| WordPress | 창작자 사이트 | 소유 미디어에 축적      | 해당 없음 | 창작자가 명시적으로 선택한 경우에만 게시 |

연동은 표의 순서대로 진행한다. 심사와 비용 없이 연속 게시까지 검증할 수 있는 채널부터 붙여 배포 구조 전체를 먼저 완성한다.

### 보류·제외 채널

| 채널             | 상태 | 사유                                                          |
| ---------------- | ---- | ------------------------------------------------------------- |
| X                | 보류 | 건당 과금이며 본문에 주소가 들어가면 단가가 크게 오른다       |
| Meta Threads     | 보류 | 일반 사용자에게 열려면 법인 서류로 하는 사업자 인증이 필요하다 |
| YouTube · TikTok | 보류 | 미디어가 필수이며 심사 전에는 게시 범위가 앱 단위로 묶인다    |
| Wix              | 보류 | 심사는 없다. WordPress 다음 확장 대상이다                     |
| Webflow · Sanity | 보류 | 문서 구조를 창작자가 정의하므로 매핑 화면이 선행되어야 한다   |
| Instagram        | 제외 | 텍스트만으로 게시할 수 없다                                   |
| Facebook         | 제외 | 개인 프로필에 올리는 방법이 없다                              |
| 네이버 블로그 · 티스토리 | 제외 | 글쓰기 기능이 종료되었다                              |

보류 채널은 화면에 자리를 두고 연동만 막는다. 제외 채널은 조건이 바뀌지 않는 한 다시 검토하지 않는다.

### 중요한 제품 판단

- 배포는 허브 서비스에 의존하지 않고 Rillroot가 직접 구현한다.
- 같은 글을 외부에 그대로 올린다고 외부 독자가 Rillroot로 자동 유입되지는 않는다. 이 기능의 MVP 가치는 창작자 편의와 활동 유지다.
- 게시글이 조각으로 이루어지므로, 연속 게시를 지원하는 채널에는 같은 구조로 배포하고 지원하지 않는 채널에서는 조각을 하나로 합친다.
- **조각의 본문은 평문이다.** 소셜 채널이 서식을 받지 않으므로 서식을 두지 않는다.
- **채널별로 본문을 다르게 쓰는 것은 허용하지 않는다.** 어떤 조각을 보낼지만 고른다. 원본이 하나여야 고쳤을 때 모든 채널이 따라온다.
- **외부 배포에 Rillroot 원문 주소를 붙이지 않는다.** 주소가 들어가면 단가가 오르는 채널이 있고, 조각 모델에서는 글 자체가 콘텐츠라 붙일 이유가 없다.
- 채널별 문자 수, 미디어, 권한 정책 등 기술·정책 세부 사항은 [게시와 동시 배포 설계](./rillroot-distribution-design.md)에서 확정한다.

## 9. Open API·MCP의 위치

Open API와 MCP는 콘텐츠를 자동으로 채우는 해결책이 아니다. 콘텐츠 초기 공급은 시드 창작자, 편집 큐레이션, 외부 배포 편의에서 시작한다.

제품·데이터 모델이 안정된 뒤 다음을 후속 후보로 둔다.

- 외부 도구에서 초안·발행·분석을 다루는 창작자 API
- MCP를 통한 에디터/에이전트 워크플로우 연동
- 공개 글 검색·임베드·추천을 위한 읽기 API

## 10. 디자인 시스템 v0.1

### 브랜드 성격

Rillroot는 작은 흐름이 시작되어 뿌리내리고, 서로 다른 글과 관점이 축적되는 공개 창작 공간이라는 인상을 지향한다. 이를 직접적인 자연물 일러스트로 묘사하지 않고, 차분한 에디토리얼 UI와 절제된 움직임으로 표현한다. 브랜드 컬러가 화면을 지배하지 않고, 중요한 행동과 상태만 선명하게 만든다.

색감과 면의 위계는 Toss Invest의 라이트 테마에서 보이는 **차분한 쿨뉴트럴 canvas 위의 깨끗한 surface 구조**를 모티브로 삼는다. 이는 금융 대시보드의 높은 정보 밀도나 금융적 시각 언어를 차용한다는 뜻이 아니다. Rillroot는 읽기 화면에서 여백과 본문 집중도를 우선한다.

### 색상

| 역할            | 라이트 모드 | 다크 모드 | 사용                            |
| --------------- | ----------- | --------- | ------------------------------- |
| App canvas      | `#F5F6F8`   | `#141518` | 앱 전체의 바탕, 페이지 여백     |
| Primary surface | `#FFFFFF`   | `#1C1D21` | 본문 영역, 카드, 입력 영역      |
| Subtle surface  | `#FAFBFC`   | `#22242A` | 호버, 보조 패널, 선택 전 컨트롤 |
| 주 텍스트       | `#191F28`   | `#F4F6F1` | 제목, 본문                      |
| 보조 텍스트     | `#8B95A1`   | `#9198A3` | 메타데이터, 보조 설명           |
| 구분선          | `#EAECF0`   | `#2C2F36` | 경계, 비활성 상태               |
| Brand Accent    | `#B8F34A`   | `#B8F34A` | CTA, 활성·선택·핵심 신호        |

색 사용 원칙: 중립색 약 90%, 브랜드 라임 약 8%, 상태색 약 2%. 브랜드 라임은 라이트·다크 모드 모두 `#B8F34A` 하나로 고정한다. 라임은 대면적 배경이 아니라 CTA, 활성 상태, 포커스, 파비콘에 우선 사용한다. 라임은 수익·상승·성공을 의미하는 상태색으로 쓰지 않으며, 의미 색상은 별도 시맨틱 토큰으로 관리한다.

### 면과 레이아웃

- `App canvas`는 화면 전체를 감싸는 기본 면이며, 콘텐츠를 카드처럼 과도하게 분절하지 않는다.
- 글 읽기와 작성의 중심 영역은 `Primary surface` 위에 둔다. 라이트 모드에서는 흰 면이 canvas에서 아주 가볍게 분리되어야 한다.
- 그림자는 최소화하고, 얇은 구분선·명도 차이·여백으로 surface 위계를 만든다.
- 읽기 화면은 넓은 여백과 안정된 본문 폭을 유지한다. 카드 밀도와 다중 패널은 창작자 대시보드·배포 관리·분석 화면에 한정해 점진적으로 사용한다.

### 로고

로고는 텍스트 없이 세 개의 라운드 모노라인으로 구성된 심볼이다. 개구리를 묘사하지 않으며, 서로 다른 길이의 세 궤적이 빈 공간과 움직임을 만든다.

- 화이트 모드: 다크 로고 `#0A0C0A`
- 다크 모드: 라이트 로고 `#F4F6F1`
- 브랜드 로고: 투명 배경 위 브랜드 라임 `#B8F34A`
- 파비콘: 라임 배경 `#B8F34A` + 다크 로고

에셋:

- [다크 로고 SVG](./rillroot-mark-dark.svg)
- [라이트 로고 SVG](./rillroot-mark-light.svg)
- [브랜드 컬러 로고 SVG](./rillroot-mark-brand.svg)
- [파비콘 SVG](./rillroot-favicon.svg)

### 모션 로고와 로딩

공통 로딩 상태에는 배경이 없는 브랜드 라임(`#B8F34A`)의 모션 로고를 사용한다. 세 라운드 모노라인은 정적 심볼과 같은 형태를 유지하되, 각기 다른 시작점에서 드로잉된다.

- 상단 선: 왼쪽에서 오른쪽으로 드로잉
- 중앙 선: 중앙에서 양 끝으로 동시에 드로잉
- 하단 선: 오른쪽에서 왼쪽으로 드로잉
- 기본 모션: 1.8초 반복, 선이 완성된 상태를 잠시 유지
- 접근성: `prefers-reduced-motion` 환경에서는 드로잉 없이 완성된 정적 마크를 표시

이 모션은 전체 페이지의 장시간 대기 화면보다 에디터 자동 저장, 포스트 발행, 외부 채널 배포, 짧은 피드 전환처럼 브랜드 피드백이 필요한 짧은 대기 상태에 사용한다.

- [브랜드 모션 로고 SVG](./rillroot-loader-light.svg)

### 구현 단위

디자인 토큰은 `packages/ui`에서 플랫폼 중립 값으로 관리한다. 웹은 CSS 변수/테마로, 향후 앱은 React Native 테마 객체로 변환한다. 로고 SVG는 웹 자산으로 우선 사용하되, 앱 출시 시에는 동일한 선 굵기·여백 규칙을 기반으로 네이티브 자산 세트를 별도 생성한다.

## 11. 성공 지표와 이벤트

초기에는 절대 목표치를 성급히 고정하지 않고, 아래 지표의 기준선을 만든 뒤 코호트별 목표를 설정한다.

| 영역        | 핵심 지표                               | 대표 이벤트                                                              |
| ----------- | --------------------------------------- | ------------------------------------------------------------------------ |
| 도달        | 월간 활성 독자, 외부 유입 비중          | `post_viewed`, `referral_arrived`                                        |
| 활성화      | 첫 글 발행률                            | `auth_completed`, `post_published`                                       |
| 창작자 유지 | 4주 내 재발행 창작자 비율               | `post_published`                                                         |
| 독자 유지   | 7일/28일 재방문 독자 비율               | `session_started`, `post_viewed`                                         |
| 배포 사용성 | 발행 시 외부 배포 선택률, 채널별 성공률 | `distribution_selected`, `distribution_succeeded`, `distribution_failed` |
| 품질        | 저장·팔로우·깊이 있는 읽기 비율         | `post_saved`, `author_followed`, `read_depth_reached`                    |

## 12. 주요 리스크와 대응

| 리스크                              | 대응 방향                                                                     |
| ----------------------------------- | ----------------------------------------------------------------------------- |
| 콘텐츠 콜드 스타트                  | 시드 창작자와 편집 큐레이션을 우선하고, 배포 편의를 창작자 확보 수단으로 사용 |
| 외부 전문 미러링으로 인한 유입 부족 | 배포 기능의 성공을 유입만으로 판단하지 않으며, 후속으로 요약·링크 모드를 실험 |
| 소셜 플랫폼 API 제약                | 채널별 권한·정책을 사전 검토하고, 발행 실패 상태와 재시도 UX를 설계           |
| 보상 어뷰즈                         | 금전 보상을 지연하고, 검증된 기여·사기 탐지·월 정산 체계를 먼저 구축          |
| 범용 소셜 서비스와의 차별화 부족    | 창작자 배포 경험, 일관된 브랜드, 공개 읽기 경험, 신뢰 가능한 향후 보상에 집중 |

## 13. 출시 순서

### Phase 1 — 읽기와 발행의 기반

모노레포의 웹 앱·API 서버·공유 계약을 먼저 구성하고, 공개 열람, Supabase Auth 인증, 에디터, 포스트·프로필, 기본 탐색, 분석 이벤트를 제공한다.

### Phase 2 — 창작자 배포 경험

Bluesky·Mastodon·LinkedIn·WordPress의 선택적 직접 연동, 발행 상태, 재시도, 기본 성과 확인을 제공한다. X는 건당 비용 정책을 정한 뒤, Meta Threads는 사업자 인증 뒤에 연동한다.

### Phase 3 — 리텐션과 큐레이션

저장·팔로우, 운영 큐레이션, 창작자 재방문 루프를 강화한다.

### Phase 4 — 검증된 수익화

팁·멤버십·바운티부터 검증하고, 조건이 충족될 때만 광고/제휴 수익 분배를 검토한다.

## 14. 아직 결정할 사항

- 첫 시드 창작자 군과 초기 편집 주제
- 언어 자동 감지, 기본 폴백, 지역별 스페인어 카피의 운영 정책
- 댓글, 반응, 저장, 팔로우 중 MVP 상호작용의 최소 범위
- 포스트 편집기 포맷과 미디어 지원 범위
- X의 건당 비용을 서비스가 부담할지, 창작자의 키를 쓰게 할지
- 콘텐츠 모더레이션, 신고, 저작권, 데이터 보관 정책
- LinkedIn 약관이 금지하는 자동 게시의 범위와 이 서비스의 해당 여부
- 쌓인 게시글을 Rillroot에서 어떤 형태로 보여 줄지
- API 계약의 공개 범위와 향후 모바일 인증·딥링크 정책
