# Rillroot 게시와 동시 배포 설계

| 항목        | 내용                                               |
| ----------- | -------------------------------------------------- |
| 문서        | 게시글 작성과 외부 채널 동시 배포 설계             |
| 상태        | 설계 확정, 구현 중                                 |
| 기준 문서   | Rillroot PRD v0.2, Rillroot 인증 설계              |
| 대상 채널   | Bluesky, Mastodon, LinkedIn, WordPress             |
| 콘텐츠 단위 | 제목과 미디어를 선택으로 가지는, 조각의 순서 있는 목록 |
| 본문 형식   | 평문                                               |
| 서버 경계   | 외부 토큰 보관과 배포 실행은 API만 맡는다          |

## 1. 범위

이 문서는 게시글의 콘텐츠 모델, 작성 화면, 외부 계정 연동, 배포 파이프라인, 배포 상태 표시를 정의한다.

글 탐색, 토픽, 검색, 저장·팔로우, 예약 발행, 발행 후 수정의 외부 반영은 범위 밖이며 해당 기능을 설계할 때 정의한다. **쌓인 게시글을 Rillroot에서 어떤 형태로 보여 줄지도 범위 밖이다.**

미디어 첨부는 이 문서에서 자리와 데이터 구조만 정의하고 업로드 구현은 다루지 않는다.

## 2. 결정 요약

| 항목                  | 결정                                                        |
| --------------------- | ----------------------------------------------------------- |
| 콘텐츠 단위           | 게시글은 하나 이상의 조각으로 이루어진다                    |
| 조각의 본문 형식      | 평문이다. 서식을 두지 않는다                                |
| 조각을 나누는 주체    | 창작자가 정한다. 길이는 조건이 아니다                       |
| 제목                  | 요구하는 채널을 켤 때만 받는다                              |
| 자동 분할             | 조각이 채널 한도를 넘을 때 그 채널에서만 일어난다           |
| 연속 게시가 없는 채널 | 조각을 하나로 합쳐 한 건으로 배포한다                       |
| 채널별 본문 수정      | 허용하지 않는다. 어떤 조각을 보낼지만 고른다                |
| 채널 선택 위치        | 작성 화면 맨 위에 둔다                                      |
| 채널 선택 기본값      | 지난 발행에서 고른 채널을 기억한다                          |
| 채널별 설정           | 한 번만 정할 것은 연동 시점에, 글마다 다른 것은 작성 화면에 |
| 요구를 못 채운 채널   | 배포 대상에서 빼고 사유를 고지한다                          |
| 연동 시점             | 채널을 켤 때 연동한다                                       |
| 연동 창               | 팝업으로 열고 작성 중인 내용을 유지한다                     |
| 만료 확인             | 채널을 켤 때와 발행 직전에 확인한다                         |
| 미리보기              | 채널별 조립 결과를 실제 모양으로 보여 준다                  |
| Rillroot 발행         | 외부 배포와 분리해 먼저 확정한다                            |
| 배포 계획             | 발행 시점에 확정해 보낼 내용과 멱등 키까지 저장한다         |
| 배포 실행             | 백그라운드 잡으로 돌린다. 화면을 닫아도 계속된다            |
| 병렬성                | 채널 사이는 병렬, 한 채널 안의 건은 순차                    |
| 큐                    | 배포 테이블을 큐로 쓴다. 별도 큐 서버를 두지 않는다         |
| 상태 전달             | API가 서버 전송 이벤트로 보낸다                             |
| 중간 실패             | 그 채널만 부분 게시로 멈추고 다른 채널은 계속한다           |
| 이미 올라간 건        | 지우지 않고 이어서 재시도한다                               |
| 실패 분류             | 재시도 가능, 재연동 필요, 콘텐츠 거부, 채널 한도로 나눈다   |

## 3. 콘텐츠 모델

### 3.1 조각

- **게시글은 조각의 순서 있는 목록이다.** 조각이 하나면 단일 게시글이고, 여럿이면 연속 게시다. 둘은 다른 종류가 아니라 같은 구조의 길이 차이다.
- **조각을 나누는 지점은 창작자가 정한다.** 한도를 채워야 다음 조각으로 넘어가는 것이 아니다.
- **조각 안의 입력 길이는 제한하지 않는다.** 채널 한도는 입력을 막는 조건이 아니라 배포 결과를 바꾸는 조건이다.
- **조각의 본문은 평문이다.** 대상 채널 넷 중 셋이 서식을 받지 않으므로 서식을 두면 대부분의 채널에서 버려진다. 서식이 필요한 채널에는 평문을 그 채널의 형식으로 내려 보낸다.
- **조각은 게시글의 유일한 진실이다.** 채널마다 다른 본문을 따로 두지 않는다.

### 3.2 제목과 미디어

- **제목은 게시글의 속성이되 선택이다.** 요구하는 채널을 켤 때만 작성 화면에 나타난다.
- **제목을 비우면 첫 조각에서 파생한다.** 창작자가 덮어쓸 수 있다.
- **미디어는 두 자리에 붙는다.** 조각에 붙는 것과 게시글 전체에 붙는 것이 다르다. 조각별로 이미지를 다는 채널이 있고, 게시글 하나에 영상 하나를 다는 채널이 있다.

### 3.3 채널 종류

채널은 입력 요구에 따라 갈리며, 하나의 작성 형식이 모든 채널을 덮지 않는다. **공통 모양은 제목과 미디어를 선택으로 가지는 조각 목록 하나이고, 채널마다 다른 것은 그중 무엇을 쓰고 무엇을 요구하는가다.**

| 종류             | 제목 | 조각을 합친 본문      | 미디어      | 글마다 받을 설정           |
| ---------------- | ---- | --------------------- | ----------- | -------------------------- |
| 짧은 글 · 연속 게시 | —    | 조각마다 한 건        | 조각별 선택 | —                          |
| 긴 글 · 단건     | —    | 게시물 본문           | 선택        | —                          |
| 정형 블로그      | 필요 | 글 본문               | 선택        | 카테고리·태그              |
| 비정형 CMS       | 필요 | 매핑된 필드           | 선택        | —                          |
| 영상             | 채널에 따라 | 설명 또는 캡션 | **필수**    | 공개범위·분류·상호작용 허용 |

- **영상 채널은 본문이 없는 채널이 아니라 미디어가 필수인 채널이다.** 텍스트는 제목과 설명으로 그대로 매핑된다.
- **비정형 CMS는 문서 구조를 창작자가 정의한다.** 어느 필드에 본문을 넣을지 우리가 알 수 없으므로 연동할 때 매핑을 받는다.

### 3.4 채널 능력

| 채널      | 한 건 한도                     | 연속 게시 | 연속 게시 방식                  |
| --------- | ------------------------------ | --------- | ------------------------------- |
| Bluesky   | 300 자소이면서 동시에 3000 바이트 | 지원      | 최상위 글과 직전 글을 함께 참조 |
| Mastodon  | 인스턴스마다 다르다            | 지원      | 직전에 발행된 글에 이어 단다    |
| LinkedIn  | 3000 자                        | 없음      | —                               |
| WordPress | 없음                           | 해당 없음 | —                               |

- **글자를 세는 방식이 채널마다 다르다.** 자소와 바이트를 동시에 만족해야 하는 채널이 있고, 바이트로만 재는 채널이 있으며, 링크를 길이와 무관하게 고정 길이로 세는 채널이 있다. 하나의 숫자로 뭉뚱그리지 않는다.
- **한도를 런타임에 조회해야 하는 채널이 있다.** 인스턴스마다 값이 다른 채널은 연동된 서버에 물어 확인하고 그 결과를 보관한다.
- **연속 게시에는 일괄 호출이 없다.** 앞 글이 발행되어 식별자가 생겨야 다음 글을 이어 붙일 수 있다.
- **연속 게시를 흉내 내지 않는다.** 지원하지 않는 채널에서 여러 건으로 나누면 서로 연결되지 않은 글이 남는다.

### 3.5 채널 능력 선언과 배포 계획

- **채널마다 능력을 선언으로 두고, 순수 함수 하나가 게시글과 선언을 받아 건 목록 또는 배포 불가 사유를 만든다.** 어댑터는 그 결과를 보내기만 하고 몇 건으로 보낼지 판단하지 않는다.
- 선언에 담는 것은 한 건 한도와 세는 방식, 연속 게시 지원 여부와 참조 방식, 본문 형식, 없으면 배포할 수 없는 것, 글마다 받을 설정, 연동 시 받을 설정, 중복을 막는 수단, 합칠 때 조각 사이에 넣을 것이다.
- **작성 화면의 고지와 실제 배포가 같은 함수를 쓴다.** 그래야 발행 전에 알린 결과와 실제 결과가 어긋나지 않는다.
- **채널을 늘릴 때 바뀌는 것은 선언과 보내기 어댑터뿐이다.** 계획, 고지, 진행 표시, 재시도는 그대로 쓴다.

### 3.6 본문 형식과 변환

- **평문을 각 채널의 형식으로 내려 보낸다.** 채널이 받는 형식은 평문, 평문에 별도 범위 주석을 더한 것, 예약 문자를 이스케이프한 평문, HTML, 채널 고유의 문서 형식으로 나뉜다.
- **링크는 서식이 아니라 평문 속 주소로 다루며 처리가 채널마다 다르다.** 위치를 바이트 범위로 직접 지정해야 하는 채널이 있고, 자동으로 링크가 되는 채널이 있으며, 미리보기 카드를 만들려면 대상 문서의 정보를 우리가 읽어 채워야 하는 채널이 있다.
- **바이트 오프셋을 요구하는 채널에서는 문자 단위로 자르지 않는다.** 비ASCII 문자에서 위치가 어긋난다.
- **예약 문자를 가진 형식에서는 본문을 그대로 보내지 않는다.** 이스케이프하지 않으면 해석이 깨진다.

## 4. 작성 화면

### 4.1 구성

```
┌ 새 글 ─────────────────────── [미리보기] [×] ┐
│ 함께 올릴 곳                                  │
│ [채널] [채널] [채널] [＋ 연결]                │
│                                               │
│ ▾ 채널                                        │
│   설정  설정  설정                            │
│   요구를 못 채운 사유                          │
├───────────────────────────────────────────────┤
│ 제목                                          │
│                                               │
│ ● 조각                                        │
│ ● 조각                                        │
│ ＋ 조각 추가            [미디어]      한도    │
├───────────────────────────────────────────────┤
│ 채널별 배포 결과 요약            [발행하기]   │
└───────────────────────────────────────────────┘
```

- **채널 선택을 맨 위에 둔다.** 한도, 제목 필요 여부, 미디어 필요 여부가 모두 채널로 정해지므로 채널을 먼저 알아야 나머지를 그릴 수 있다.
- **Rillroot 자체는 언제나 기본 대상이다.** 그래서 채널을 앞에 두어도 글을 쓰기 전에 연동을 요구받지 않는다. 채널 선택은 관문이 아니라 어디에 더 보낼지를 고르는 것이다.
- **설정이 필요한 채널만 줄이 생긴다.** 추가 설정이 없는 채널은 칩만 두고, 요구가 있는 채널을 켤 때만 그 아래가 펼쳐진다.
- **채널별 설정을 접거나 다른 화면으로 숨기지 않는다.** 눈앞에 있어야 빠진 것을 발행 전에 알아챈다.
- **제목 칸은 그것을 요구하는 채널을 켤 때만 나타난다.**
- **아무 채널도 켜지 않으면 조각만 남는다.**
- **글자 서식과 첨부 종류를 늘어놓는 줄을 두지 않는다.** 본문은 평문이고 미디어는 한 자리에서 받는다.

### 4.2 채널 선택과 연동

1. 연동되지 않은 채널을 켜면 그 자리에서 연동을 시작한다.
2. **연동 창은 팝업으로 연다.** 현재 페이지를 이동시키면 작성 중인 내용이 사라진다.
3. **연동할 때 그 채널이 한 번만 정하면 되는 것을 받는다.** 서버 주소, 사이트 주소와 접속 수단, 문서 구조 매핑이 여기에 해당한다. 받은 값은 연동 정보에 보관하고 발행할 때 다시 묻지 않는다.
4. 연동이 끝나면 팝업이 결과만 돌려주고 작성 화면은 그대로 유지된다.
5. **팝업이 차단될 수 있으므로 작성 중인 내용은 연동을 시작하기 전에 저장한다.**
6. **만료를 두 번 확인한다.** 채널을 켤 때는 저장된 만료 시각으로 판단하고, 발행 직전에는 실제 호출로 판단한다. 창작자가 채널 쪽에서 권한을 거두면 만료 시각과 무관하게 끊기므로 호출 전에는 알 수 없다.
7. 만료되었거나 권한이 끊긴 채널은 **재연동이 필요한 상태**로 표시하고, 재시도 대상으로 다루지 않는다.

### 4.3 채널별 설정과 요구

- **글마다 달라지는 설정은 작성 화면에서 받는다.** 공개 범위, 분류, 상호작용 허용 여부, 표기 의무가 여기에 해당하며 연동 시점으로 미룰 수 없다.
- **요구를 못 채운 채널은 배포 대상에서 빠지고 사유를 그 채널 줄에 적는다.** 제목이 없거나 미디어가 없는 경우가 여기에 해당한다. 배포를 막는 것이 아니라 그 채널만 빠지는 것이다.
- **채널이 화면 구성을 규정하는 경우 그 규정을 따른다.** 기본값을 두지 말라거나, 특정 항목을 꺼진 상태로 시작하라거나, 보내기 전에 결과를 보이라는 요구가 여기에 해당한다. **이런 채널의 설정에는 지난 발행을 기억하는 규칙을 적용하지 않는다.**

### 4.4 한도 표현

- **평소에는 한도를 표시하지 않는다.** 한도 표시는 선택된 채널 중 가장 좁은 한도를 조각이 넘는 순간에만 나타난다.
- 넘어선 지점에 경계를 긋고 그 뒤 구간을 다르게 칠해, 어디부터 다음 건으로 넘어가는지 보여 준다.
- **입력을 막지 않는다.** 넘어선 상태도 유효한 상태이며, 그 결과를 배포 결과 요약이 설명한다.
- 한도 계산은 채널마다 그 채널의 규칙으로 따로 한다.

### 4.5 미리보기와 배포 결과 요약

- **미리보기는 채널별 조립 결과를 실제 모양으로 보여 준다.** 나누어지는 채널에서는 나뉜 건들로, 합쳐지는 채널에서는 합쳐진 한 건으로 그린다.
- **미리보기는 한 번 쓴 글이 채널마다 어떻게 조립되는지를 보이는 장치다.** 채널마다 따로 쓴 것을 확인하는 화면이 아니다.
- 배치는 넓은 화면에서 본문 옆에 두고 좁은 화면에서는 전환으로 연다. 기본은 접어 둔다.
- **발행 버튼 위에 채널별 결과를 한 줄로 고지한다.** 각 채널에서 몇 건이 되는지, 조각이 합쳐지는지 나뉘는지, 빠지는 채널이 있는지를 발행 전에 알린다.
- 이 줄은 발행 결과에 대한 약속이며, 창작자가 발행 버튼을 누른 뒤 예상과 다른 결과를 만나지 않게 하는 장치다.

### 4.6 채널별 조각 선택

- **채널마다 본문을 다르게 쓰는 것은 허용하지 않는다.** 대신 **어떤 조각을 보낼지만** 고르게 한다.
- **기본은 모든 조각을 모든 채널에 보내는 것이며, 선택하는 자리는 문제가 있을 때만 나타난다.** 합친 결과가 그 채널의 한도를 넘는 경우가 여기에 해당한다.
- 선택하지 않으면 그 채널은 배포 대상에서 빠진다.
- **원본을 고치면 모든 채널이 따라온다.** 채널마다 본문이 따로 살아 있으면 한쪽을 고쳐도 다른 쪽이 따라오지 않으므로, 진실을 조각 하나로 유지한다.

## 5. 배포 진행 표시

```
 진행  채널   2/3
 완료  채널   보기
 대기  채널   1/1
 중단  채널   재연동 필요
 제외  채널   사유
```

- **발행을 누르면 작성 화면이 진행 화면으로 바뀐다.** 채널마다 한 행을 두고 상태와 진척을 표시한다.
- **진척의 분모는 그 채널에서 만들어질 건수다.** 조각 수가 아니라 채널별로 계산된 건수를 쓴다.
- **화면을 닫아도 배포는 계속된다.** 진행 화면은 실행 주체가 아니라 관찰자다.
- 모든 채널이 끝나면 성공한 채널의 외부 주소와 실패한 채널의 사유를 함께 보여 준다.
- **Rillroot 발행은 이 화면이 열리기 전에 이미 끝나 있다.** 외부가 모두 실패해도 Rillroot의 게시글은 남는다.

## 6. 데이터 모델

### 6.1 테이블

| 테이블                   | 역할                                                                 |
| ------------------------ | -------------------------------------------------------------------- |
| `posts`                  | 게시글. 작성자, 언어, 제목, 발행 시각                                |
| `post_segments`          | 조각. 게시글 참조, 순서, 본문                                        |
| `post_media`             | 미디어. 게시글 참조, 조각 참조, 종류, 저장 위치, 크기와 길이         |
| `social_connections`     | 외부 계정 연동. 사용자, 채널, 채널 쪽 계정 식별자, 토큰, 만료, 상태, 연동 설정 |
| `channel_app_credentials` | 서버마다 따로 등록해야 하는 채널의 앱 자격 증명                      |
| `distributions`          | 게시글과 채널의 조합. 상태, 실패 분류, 시도 횟수, 다음 시도 시각, 글마다 받은 설정, 보낼 조각 목록 |
| `distribution_items`     | 실제로 만들어질 외부 게시물. 순서, 보낼 내용, 중복 방지 키, 외부 식별자, 외부 주소, 상태 |

- **발행 시각이 비어 있으면 초안이다.** 초안과 발행글을 다른 테이블로 나누지 않는다.
- **제목은 요구하는 채널에만 쓰이며 비어 있을 수 있다.**
- **미디어의 조각 참조는 비어 있을 수 있다.** 비어 있으면 게시글 전체에 붙고, 채워져 있으면 그 조각에 붙는다.
- **상태는 게시글이 아니라 게시글과 채널의 조합에 둔다.** 그래야 한 채널만 실패한 상태를 표현할 수 있다.
- **외부 게시물은 조각과 일대일이 아니다.** 조각이 채널 한도를 넘어 나뉘면 항목이 늘고, 조각을 합치는 채널에서는 줄어든다.
- **실제로 보낸 내용을 항목에 남긴다.** 남기지 않으면 재시도할 때 같은 내용이 간다고 보장할 수 없고, 발행 후 원본을 고쳤을 때 올라간 것과 대조할 수 없다.
- **연동은 사용자와 채널마다 하나만 둔다.** 같은 채널의 여러 계정에 같은 글을 올리는 것은 채널 약관이 금지한다.
- **서버마다 앱을 따로 등록해야 하는 채널은 그 자격 증명을 보관해 다시 쓴다.** 연동할 때마다 등록하지 않는다.
- **토큰은 암호화해 저장한다.**
- **저장소는 공개 주소를 낼 수 있어야 한다.** 미디어를 주소로 가져가는 채널이 있다.

### 6.2 접근 제어

- 인증 설계의 규칙을 그대로 따른다. 테이블을 만드는 마이그레이션에서 RLS를 켜고 정책은 두지 않으며, 모든 접근은 API를 거친다.
- **배포 상태도 클라이언트가 직접 구독하지 않는다.** API가 읽어 전달한다.

## 7. 배포 파이프라인

### 7.1 실행 순서

1. Rillroot에 게시글을 발행한다. 이 단계가 성공해야 배포가 시작된다.
2. **계획 함수를 돌려 채널별 건 목록을 만들고, 보낼 내용과 중복 방지 키까지 담아 항목을 미리 전부 만든다.** 이 시점에 배포 결과가 확정된다.
3. 선택된 채널마다 배포 행을 만든다. **이 행이 곧 작업 큐다.**
4. 워커가 대기 중인 행을 가져간다. 여러 워커가 같은 행을 집지 않도록 잠금을 건다.
5. **채널 사이는 병렬로 실행한다.**
6. **한 채널 안에서는 항목을 순서대로 하나씩 보낸다.** 앞 글의 식별자가 있어야 다음 글을 이어 붙일 수 있다.
7. 각 건이 끝날 때마다 항목과 상태를 기록한다.

- **계획을 미리 확정해 저장하므로 진척의 분모가 실제 행 수가 되고, 재시도가 저장된 내용을 그대로 다시 보내며, 발행 후 원본을 고쳐도 이미 계획된 배포가 영향받지 않는다.**
- **발행 시각을 클라이언트가 정하는 채널에서는 큐에 넣는 시각이 아니라 실제로 보내는 시각을 쓴다.** 그러지 않으면 글이 과거에 묻혀 드러나지 않는다.

### 7.2 실패 분류

| 분류        | 뜻                               | 처리                             |
| ----------- | -------------------------------- | -------------------------------- |
| 재시도 가능 | 일시적 오류, 네트워크 실패       | 간격을 늘려 가며 다시 시도한다   |
| 재연동 필요 | 토큰 만료, 권한 철회             | 재시도하지 않고 창작자에게 알린다 |
| 콘텐츠 거부 | 길이, 형식, 중복으로 채널이 거부 | 재시도하지 않는다                |
| 채널 한도   | 호출 한도 초과                   | 한도가 풀리는 시각 이후로 미룬다 |

- **재연동이 필요한 실패를 재시도 루프에 넣지 않는다.** 사람이 개입해야 풀리는 실패이므로 재시도는 대기 시간만 늘린다.
- 한 채널의 실패는 다른 채널의 실행을 멈추지 않는다.
- **호출 한도가 계정별과 주소별로 따로 걸리는 채널이 있다.** 여러 창작자가 같은 서버를 쓰면 우리 쪽 주소 하나로 요청이 몰려 서로를 막을 수 있으므로, 같은 서버로 가는 호출은 간격을 두고 보낸다.

### 7.3 중간 실패와 재시도

- **연속 게시 도중 실패하면 그 채널은 거기서 멈춘다.** 다음 건은 앞 건의 식별자를 필요로 하므로 건너뛸 수 없다. 이 상태는 실패가 아니라 **부분 게시**이며 몇 건까지 올라갔는지를 함께 기록한다.
- **이미 올라간 건은 지우지 않는다.** 마지막으로 성공한 외부 식별자를 저장해 두고, 재시도할 때 그 지점부터 이어 붙인다.
- **중복 게시를 막는 수단은 채널마다 다르므로 계획 단계에서 만들어 저장한다.** 식별자를 직접 지정할 수 있는 채널에서는 그 식별자를, 요청 단위 중복 방지 수단이 있는 채널에서는 그 값을 쓴다. 어느 쪽도 없는 채널에서는 재시도 전에 채널에 조회해 이미 올라갔는지 확인한다.
- **응답을 받지 못한 호출을 그대로 다시 보내지 않는다.**

## 8. 채널 연동 요건

| 채널      | 심사·서류 | 사전 준비                        | 연동할 때 받을 것            | 토큰                     |
| --------- | --------- | -------------------------------- | ---------------------------- | ------------------------ |
| Bluesky   | 없음      | 클라이언트 정보를 도메인에 게시  | 없음                         | 짧은 접근 토큰과 갱신 토큰 |
| Mastodon  | 없음      | 없음                             | 서버 주소                    | 만료되지 않는다          |
| LinkedIn  | 없음      | 앱을 페이지에 연결하고 승인받는다 | 없음                         | 60일, 갱신 불가          |
| WordPress | 없음      | 앱 등록                          | 사이트 주소와 접속 수단      | 방식에 따라 다르다       |

- **연동 순서는 Bluesky, Mastodon, LinkedIn, WordPress로 한다.** 심사와 비용 없이 연속 게시까지 검증할 수 있는 채널부터 붙여 파이프라인 전체를 먼저 완성한다.
- **Bluesky는 사전 등록도 심사도 없다.** 클라이언트 정보를 도메인에 올리면 어느 서버의 사용자든 바로 연결된다. 계정에서 서버 주소를 찾아가는 절차가 표준으로 정해져 있어 자체 운영 서버도 같은 방식으로 동작한다.
- **Mastodon은 서버마다 앱을 따로 등록해야 한다.** 등록에는 인증이 필요 없고 즉시 끝나지만, 받은 자격 증명을 보관해 다시 써야 한다. 한도와 호출 제한도 서버마다 다르므로 연동한 서버에 물어 확인한다.
- **LinkedIn은 갱신 토큰을 일반 앱에 주지 않는다.** 60일마다 창작자가 다시 연동해야 하며, 이는 오류가 아니라 정상 주기다.
- **LinkedIn에서 자기 글에 댓글을 다는 권한은 별도 심사 대상이며 등록된 법인을 요구한다.** 그 권한 없이는 연속 게시를 만들 수 없으므로 합치기를 기본으로 둔다.
- **LinkedIn은 요청에 버전을 명시해야 하고 각 버전이 약 1년 뒤 폐기된다.** 주기적으로 갱신하지 않으면 호출이 끊긴다.
- **WordPress는 접속 경로를 하드코딩하지 않는다.** 사이트가 알려 주는 위치를 읽어 찾고, 주소 설정에 따라 형태가 달라지는 것을 처리한다. 보안 설정이 접근을 막는 경우가 흔하므로 원인을 알리는 안내가 필요하다.

### 8.1 보류 채널

| 채널    | 막는 것                                                     | 풀리는 조건                          |
| ------- | ----------------------------------------------------------- | ------------------------------------ |
| X       | 건당 과금이며 본문에 주소가 들어가면 단가가 크게 오른다     | 비용 부담 주체 결정                  |
| Threads | 일반 사용자에게 열려면 법인 서류로 하는 사업자 인증이 필요하다 | 사업자 인증 후 심사                  |
| YouTube | 검증 전 신규 사용자 수가 프로젝트 단위로 묶이고 되돌릴 수 없다. 업로드 한도도 앱 전체로 묶인다 | 계정 검증과 한도 확장 심사           |
| TikTok  | 심사 전에는 게시물이 비공개로 강제되고 하루에 게시할 수 있는 사용자 수가 앱 전체로 묶인다 | 심사. 작성 화면의 규정 준수가 심사 대상 |
| Wix     | 없다. 목록에 올리지 않는 앱은 심사를 거치지 않는다          | 확장 우선순위에서 첫 번째            |
| Webflow | 외부에 배포하는 앱은 비공개여도 심사를 거친다               | 심사. 더해서 문서 구조 매핑 화면 필요 |
| Sanity  | 서드파티 인증이 승인된 상대에게만 열려 있어 사실상 토큰을 직접 받아야 한다 | 매핑 화면과 토큰 보관 방식 결정      |

- **비용이나 서류로 막힌 채널도 화면에 자리를 두고 연동만 막는다.** 비활성 상태로 표시하고 준비 중임을 알린다.
- **X는 계정 하나만 연동한다.** 한 서비스가 여러 계정에 같은 글을 올리게 하는 것을 약관이 금지한다.
- **영상 채널은 콘텐츠 모델이 아니라 운영 한도가 막는다.** 텍스트는 제목과 설명으로 그대로 매핑되므로 한도가 풀리면 선언과 어댑터만 더하면 된다.

### 8.2 제외 채널

| 채널             | 제외 사유                                                    |
| ---------------- | ------------------------------------------------------------ |
| Instagram        | 텍스트만으로 게시할 수 없고 미디어가 반드시 있어야 한다      |
| Facebook         | 개인 프로필에 올리는 방법이 없어지고 페이지에만 올릴 수 있다 |
| 네이버 블로그    | 글쓰기 기능이 종료되었다                                     |
| 티스토리         | 외부 연동 기능 전체가 종료되었다                             |
| Medium           | 신규 연동을 더 이상 받지 않는다                              |

- **글쓰기 기능이 닫힌 채널은 다시 열릴 것으로 가정하지 않는다.** 자동 대량 게시를 막으려는 조치이므로 되돌아오지 않는다.

## 9. API

- **`POST /posts`:** 제목과 조각 목록을 받아 게시글을 만든다. 발행하거나 초안으로 둔다. 요청 본문을 받는 첫 경로이므로 검증 규약을 여기서 정한다.
- **초안 경로:** 내 글 목록 조회, 수정, 삭제, 발행. 수정과 삭제는 작성자 본인의 초안에만 허용한다.
- **배포 요청:** 게시글 발행 요청에 선택된 채널과 채널별 설정을 함께 받아 배포 행과 항목을 만든다. 별도 경로로 나누지 않는다.
- **`GET /connections`:** 내 연동 목록과 각 채널의 상태를 반환한다. 토큰은 반환하지 않는다.
- **연동 시작과 복귀:** 채널별 인가 주소를 발급하고 복귀 지점에서 토큰을 교환한다. 복귀 경로 파라미터는 내부 경로만 허용한다.
- **배포 상태 구독:** 게시글의 채널별 상태를 서버 전송 이벤트로 보낸다. 연결이 끊기면 조회로 대체할 수 있어야 한다.
- **재시도:** 특정 배포를 다시 시도한다. 부분 게시는 남은 지점부터 이어 붙인다.
- **공개 조회 경로는 노출 방식을 정한 뒤에 정의한다.**

## 10. 구현 단계

| 단계             | 내용                                                            | 확인 기준                                          |
| ---------------- | --------------------------------------------------------------- | -------------------------------------------------- |
| 1. 작성과 저장   | `posts`·`post_segments` 마이그레이션, 검증 규약, 작성·초안 경로, 조각 단위 작성 화면 | 조각 여럿을 가진 글이 순서대로 쌓이고 초안이 이어진다 |
| 2. 노출 방식     | 쌓인 글을 어떤 형태로 보여 줄지 정하고 조회 경로를 만든다       | 별도 논의 후 착수한다                              |
| 3. 연동 기반     | `social_connections`, 앱 자격 증명 보관, 토큰 암호화, 팝업 연동 흐름 | 연동과 해제가 되고 작성 중인 내용이 유지된다       |
| 4. 배포 기반     | 채널 능력 선언과 계획 함수, `distributions`·`distribution_items`, 워커, 상태 전달 | 계획이 두 경로를 모두 만들고 채널 하나로 끝까지 성공한다 |
| 5. Bluesky       | 자소와 바이트 동시 계산, 링크 범위, 연속 게시, 중복 방지, 재시도 | 부분 실패 후 이어서 재시도가 된다                  |
| 6. Mastodon      | 서버별 앱 등록과 보관, 한도 조회, 연속 게시, 중복 방지          | 서로 다른 두 서버에 연동과 배포가 된다             |
| 7. LinkedIn      | 조각 합치기, 한도 초과 시 제외, 재연동 안내                     | 합친 결과가 고지와 같다                            |
| 8. WordPress     | 접속 경로 탐색, 본문 변환, 제목 입력                            | 두 연동 방식 모두에서 글이 올라간다                |
| 9. 화면 완성     | 채널 선택, 채널별 설정, 한도 표현, 미리보기, 배포 결과 요약, 진행 화면 | 발행 전 고지와 실제 결과가 일치한다                |

## 11. 남은 결정

- 배포 결과를 Rillroot에서 어떤 형태로 보여 줄지
- LinkedIn 약관이 금지하는 자동 게시의 범위와 이 서비스의 해당 여부
- 발행 후 수정과 삭제를 외부 채널에 반영할지 여부
- 예약 발행 도입 여부와 채널 약관상의 허용 범위
- 미디어 업로드의 지원 범위와 채널별 변환 규칙
- 비정형 CMS를 도입할 때 문서 구조 매핑을 받는 화면
- X의 건당 비용을 서비스가 부담할지, 창작자의 키를 쓰게 할지
- 조각을 나누지 않고 긴 글을 쓴 경우의 보조 분할 도입 여부
