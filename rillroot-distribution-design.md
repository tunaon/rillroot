# Rillroot 게시와 동시 배포 설계

| 항목          | 내용                                                     |
| ------------- | -------------------------------------------------------- |
| 문서          | 게시글 작성과 외부 채널 동시 배포 설계                   |
| 상태          | 설계 확정, 구현 전                                       |
| 기준 문서     | Rillroot PRD v0.2, Rillroot 인증 설계                    |
| 대상 채널     | Bluesky, Meta Threads, LinkedIn, X                       |
| 콘텐츠 단위   | 조각을 이어 붙인 게시글                                  |
| 서버 경계     | 외부 토큰 보관과 배포 실행은 API만 맡는다                |

## 1. 범위

이 문서는 게시글의 콘텐츠 모델, 작성 화면, 외부 계정 연동, 배포 파이프라인, 배포 상태 표시를 정의한다.

글 탐색, 토픽, 검색, 저장·팔로우, 예약 발행, 발행 후 수정의 외부 반영, 미디어 첨부는 범위 밖이며 해당 기능을 설계할 때 정의한다.

## 2. 결정 요약

| 항목             | 결정                                                          |
| ---------------- | ------------------------------------------------------------- |
| 콘텐츠 단위      | 게시글은 하나 이상의 조각으로 이루어진다                      |
| 조각을 나누는 주체 | 창작자가 정한다. 길이는 조건이 아니다                       |
| 자동 분할        | 조각이 채널 한도를 넘을 때 그 채널에서만 일어난다             |
| 연속 게시가 없는 채널 | 조각을 하나로 합쳐 한 건으로 배포한다                    |
| 채널 선택 위치   | 작성 다이얼로그 안에 둔다. 사전 선택 단계를 두지 않는다       |
| 채널 선택 기본값 | 지난 발행에서 고른 채널을 기억한다                            |
| 연동 시점        | 채널을 켤 때 연동한다                                         |
| 연동 창          | 팝업으로 열고 작성 중인 내용을 유지한다                       |
| 만료 확인        | 채널을 켤 때와 발행 직전에 확인한다                           |
| X                | 화면에 자리를 두고 연동은 막는다                              |
| Rillroot 발행    | 외부 배포와 분리해 먼저 확정한다                              |
| 배포 실행        | 백그라운드 잡으로 돌린다. 화면을 닫아도 계속된다              |
| 병렬성           | 채널 사이는 병렬, 한 채널 안의 조각은 순차                    |
| 큐               | 배포 테이블을 큐로 쓴다. 별도 큐 서버를 두지 않는다           |
| 상태 전달        | API가 서버 전송 이벤트로 보낸다                               |
| 중간 실패        | 그 채널만 부분 게시로 멈추고 다른 채널은 계속한다             |
| 이미 올라간 조각 | 지우지 않고 이어서 재시도한다                                 |
| 실패 분류        | 재시도 가능, 재연동 필요, 콘텐츠 거부, 채널 한도로 나눈다     |

## 3. 콘텐츠 모델

### 3.1 조각

- **게시글은 조각의 순서 있는 목록이다.** 조각이 하나면 단일 게시글이고, 여럿이면 연속 게시다. 둘은 다른 종류가 아니라 같은 구조의 길이 차이다.
- **조각을 나누는 지점은 창작자가 정한다.** 한도를 채워야 다음 조각으로 넘어가는 것이 아니다.
- **조각 안의 입력 길이는 제한하지 않는다.** 채널 한도는 입력을 막는 조건이 아니라 배포 결과를 바꾸는 조건이다.
- **Rillroot 화면에서는 조각들을 이어진 하나의 게시글로 보여 준다.**

### 3.2 채널 능력

| 채널     | 한 건 한도                | 연속 게시 | 연속 게시 방식                    |
| -------- | ------------------------- | --------- | --------------------------------- |
| Bluesky  | 300 자소, 동시에 3000 바이트 | 지원      | 최상위 글과 직전 글을 함께 참조   |
| Threads  | 500 자                    | 지원      | 직전에 발행된 글에 이어 단다      |
| LinkedIn | 3000 자                   | 없음      | —                                 |
| X        | 280 자                    | 지원      | 직전에 발행된 글에 이어 단다      |

- **글자 수 계산은 채널마다 다르다.** X는 문자마다 가중치가 다르고 링크는 길이와 무관하게 고정 길이로 계산한다. Bluesky는 자소 수와 바이트 수를 동시에 만족해야 한다. 하나의 숫자로 뭉뚱그리지 않는다.
- **연속 게시에는 일괄 호출이 없다.** 앞 글이 발행되어 식별자가 생겨야 다음 글을 이어 붙일 수 있다.
- **링크는 자동으로 풀리지 않는다.** Bluesky는 링크 위치를 바이트 범위로 직접 지정해야 하고, 미리보기 카드는 대상 문서의 메타데이터를 우리가 읽어 구성해야 한다.

## 4. 작성 화면

### 4.1 구성

```
┌ 새 게시글 ──────────────────────────────────────┐
│ ● 조각                                           │
│ ● 조각                                           │
│ ＋ 조각 추가                                      │
├──────────────────────────────────────────────── │
│ 배포  [Bluesky] [Threads] [LinkedIn] [X 준비 중] │
│ 채널별 배포 결과 요약                             │
│                                       [게시하기] │
└─────────────────────────────────────────────────┘
```

- **채널 선택은 작성 다이얼로그 안에 둔다.** 작성 전에 채널을 고르게 하면 아직 아무 채널도 연동하지 않은 창작자가 글을 쓰기 전에 연동을 요구받는다.
- **지난 발행에서 고른 채널을 기억한다.** 첫 입력부터 한도 표현이 정확해지므로 사전 선택 단계가 필요 없다.
- **X는 자리를 두고 연동을 막는다.** 비활성 상태로 표시하고 준비 중임을 알린다.

### 4.2 채널 선택과 연동

1. 연동되지 않은 채널을 켜면 그 자리에서 연동을 시작한다.
2. **연동 창은 팝업으로 연다.** 현재 페이지를 이동시키면 작성 중인 내용이 사라진다.
3. 연동이 끝나면 팝업이 결과만 돌려주고 작성 화면은 그대로 유지된다.
4. **팝업이 차단될 수 있으므로 작성 중인 내용은 연동을 시작하기 전에 저장한다.**
5. **만료를 두 번 확인한다.** 채널을 켤 때는 저장된 만료 시각으로 판단하고, 발행 직전에는 실제 호출로 판단한다. 창작자가 채널 쪽에서 권한을 거두면 만료 시각과 무관하게 끊기므로 호출 전에는 알 수 없다.
6. 만료되었거나 권한이 끊긴 채널은 **재연동이 필요한 상태**로 표시하고, 재시도 대상으로 다루지 않는다.

### 4.3 한도 표현

- **평소에는 한도를 표시하지 않는다.** 한도 표시는 선택된 채널 중 가장 좁은 한도를 조각이 넘는 순간에만 나타난다.
- 넘어선 지점에 경계를 긋고 그 뒤 구간을 다르게 칠해, 어디부터 다음 건으로 넘어가는지 보여 준다.
- **입력을 막지 않는다.** 넘어선 상태도 유효한 상태이며, 그 결과를 배포 결과 요약이 설명한다.
- 한도 계산은 채널마다 그 채널의 규칙으로 따로 한다.

### 4.4 배포 결과 요약

- **발행 버튼 위에 채널별 결과를 한 줄로 고지한다.** 각 채널에서 몇 건이 되는지, 조각이 합쳐지는지 나뉘는지를 발행 전에 알린다.
- 이 줄은 발행 결과에 대한 약속이며, 창작자가 발행 버튼을 누른 뒤 예상과 다른 결과를 만나지 않게 하는 장치다.
- 연속 게시가 없는 채널에서 합친 결과가 그 채널의 한도를 넘으면 배포 대상에서 제외하고 그 사유를 이 줄에 표시한다.

## 5. 배포 진행 표시

```
 진행  Bluesky    2/3 조각
 완료  Threads    보기
 진행  LinkedIn   1/1
 중단  X          재연동 필요
```

- **발행을 누르면 작성 화면이 진행 화면으로 바뀐다.** 채널마다 한 행을 두고 상태와 진척을 표시한다.
- **진척의 분모는 그 채널에서 만들어질 건수다.** 조각 수가 아니라 채널별로 계산된 건수를 쓴다.
- **화면을 닫아도 배포는 계속된다.** 진행 화면은 실행 주체가 아니라 관찰자다.
- 모든 채널이 끝나면 성공한 채널의 외부 주소와 실패한 채널의 사유를 함께 보여 준다.
- **Rillroot 발행은 이 화면이 열리기 전에 이미 끝나 있다.** 외부가 모두 실패해도 Rillroot의 게시글은 남는다.

## 6. 데이터 모델

### 6.1 테이블

| 테이블               | 역할                                                              |
| -------------------- | ----------------------------------------------------------------- |
| `posts`              | 게시글. 작성자, 언어, 공개 범위, 발행 시각                        |
| `post_segments`      | 조각. 게시글 참조, 순서, 본문                                     |
| `social_connections` | 외부 계정 연동. 사용자, 채널, 채널 쪽 계정 식별자, 토큰, 만료, 상태 |
| `distributions`      | 게시글과 채널의 조합. 상태, 실패 분류, 시도 횟수, 다음 시도 시각  |
| `distribution_items` | 실제로 만들어진 외부 게시물. 순서, 외부 식별자, 외부 주소, 상태   |

- **상태는 게시글이 아니라 게시글과 채널의 조합에 둔다.** 그래야 한 채널만 실패한 상태를 표현할 수 있다.
- **외부 게시물은 조각과 일대일이 아니다.** 조각이 채널 한도를 넘어 나뉘면 항목이 늘고, 조각을 합치는 채널에서는 줄어든다.
- **연동은 사용자와 채널마다 하나만 둔다.** 같은 채널의 여러 계정을 동시에 고르게 하는 것은 채널 약관이 금지한다.
- **토큰은 암호화해 저장한다.**

### 6.2 접근 제어

- 인증 설계의 규칙을 그대로 따른다. 테이블을 만드는 마이그레이션에서 RLS를 켜고 정책은 두지 않으며, 모든 접근은 API를 거친다.
- **배포 상태도 클라이언트가 직접 구독하지 않는다.** API가 읽어 전달한다.

## 7. 배포 파이프라인

### 7.1 실행 순서

1. Rillroot에 게시글을 발행한다. 이 단계가 성공해야 배포가 시작된다.
2. 선택된 채널마다 배포 행을 만든다. **이 행이 곧 작업 큐다.**
3. 워커가 대기 중인 행을 가져간다. 여러 워커가 같은 행을 집지 않도록 잠금을 건다.
4. **채널 사이는 병렬로 실행한다.**
5. **한 채널 안에서는 조각을 순서대로 하나씩 발행한다.** 앞 글의 식별자가 있어야 다음 글을 이어 붙일 수 있다.
6. 각 건이 끝날 때마다 항목과 상태를 기록한다.

### 7.2 실패 분류

| 분류        | 뜻                                      | 처리                                |
| ----------- | --------------------------------------- | ----------------------------------- |
| 재시도 가능 | 일시적 오류, 네트워크 실패              | 간격을 늘려 가며 다시 시도한다      |
| 재연동 필요 | 토큰 만료, 권한 철회                    | 재시도하지 않고 창작자에게 알린다   |
| 콘텐츠 거부 | 길이, 형식, 중복으로 채널이 거부        | 재시도하지 않는다                   |
| 채널 한도   | 호출 한도 초과                          | 한도가 풀리는 시각 이후로 미룬다    |

- **재연동이 필요한 실패를 재시도 루프에 넣지 않는다.** 사람이 개입해야 풀리는 실패이므로 재시도는 대기 시간만 늘린다.
- 한 채널의 실패는 다른 채널의 실행을 멈추지 않는다.

### 7.3 중간 실패와 재시도

- **연속 게시 도중 실패하면 그 채널은 거기서 멈춘다.** 다음 조각은 앞 조각의 식별자를 필요로 하므로 건너뛸 수 없다. 이 상태는 실패가 아니라 **부분 게시**이며 몇 건까지 올라갔는지를 함께 기록한다.
- **이미 올라간 건은 지우지 않는다.** 마지막으로 성공한 외부 식별자를 저장해 두고, 재시도할 때 그 지점부터 이어 붙인다.
- **중복 게시를 막는 수단은 채널마다 다르다.** 식별자를 직접 지정할 수 있는 채널에서는 요청 식별자로 중복을 막고, 그렇지 않은 채널에서는 재시도 전에 채널에 조회해 이미 올라갔는지 확인한다. 응답을 받지 못한 호출을 그대로 다시 보내지 않는다.

## 8. 채널 연동 요건

| 채널     | 앱 심사 | 사전 준비                                | 토큰 수명                          | 비용     |
| -------- | ------- | ---------------------------------------- | ---------------------------------- | -------- |
| Bluesky  | 없음    | 클라이언트 메타데이터를 도메인에 게시     | 짧은 접근 토큰과 갱신 토큰          | 없음     |
| Threads  | 필요    | 개인정보처리방침·데이터 삭제 안내, 화면 녹화 | 60일, 갱신 가능                 | 없음     |
| LinkedIn | 없음    | 회사 페이지 보유와 앱 검증                | 60일, 갱신 불가                    | 없음     |
| X        | 개발자 계정 승인 | 결제 수단 등록                    | 짧은 접근 토큰과 갱신 토큰          | 건당 과금 |

- **연동 순서는 Bluesky, Threads, LinkedIn, X로 한다.** 심사와 비용 없이 연속 게시까지 검증할 수 있는 채널부터 붙여 파이프라인 전체를 먼저 완성한다.
- **Threads 심사는 리드 타임이 가장 길다.** 심사 제출에는 동작하는 화면이 필요하므로 구현과 순서를 함께 잡는다.
- **LinkedIn은 갱신 토큰을 일반 앱에 주지 않는다.** 60일마다 창작자가 다시 연동해야 하며, 이는 오류가 아니라 정상 주기다.
- **LinkedIn에서 자기 글에 댓글을 다는 권한은 별도 심사 대상이다.** 그 권한 없이는 연속 게시를 흉내 낼 수 없으므로 합치기를 기본으로 둔다.
- **X는 계정 하나만 연동한다.** 한 서비스가 여러 X 계정에 같은 글을 올리게 하는 것을 약관이 금지한다.

### 확장 후보

MVP 범위 밖이며, 채널을 늘릴 때 먼저 검토할 대상으로만 기록한다.

| 채널            | 후보인 이유                                                    | 도입할 때 다룰 것                                                          |
| --------------- | -------------------------------------------------------------- | -------------------------------------------------------------------------- |
| Mastodon        | 텍스트 중심이고 연속 게시를 지원하며 심사와 비용이 없다        | 서버가 인스턴스로 나뉘어 있다                                              |
| Tumblr          | 한 게시물이 여러 블록으로 이루어져 조각 구조와 맞는다          | 대화형 피드가 아니라 리블로그 중심이라 성격이 다르다                       |
| Telegram 채널   | 심사와 비용이 없고 연동이 단순하다                             | 배포 단위가 개인 계정이 아니라 창작자가 소유한 채널이다                    |

- **Mastodon의 연동 방식은 이미 정한 채널들과 같다.** 직전 글을 참조해 연속 게시를 만들고 한 건 기본 한도는 500자다. 다만 인스턴스마다 앱을 따로 등록해야 하고 한도와 호출 제한도 인스턴스마다 다르므로, 창작자가 자기 인스턴스 주소를 입력하는 단계가 필요하다. Bluesky의 서버 탐색 구조와 성격이 같아 그 구현을 이어 쓸 수 있다.
- **Tumblr는 텍스트 블록당 4000자 남짓을 담는다.** 조각을 블록으로 옮기면 한 게시물 안에서 순서가 유지된다.
- **Telegram은 글이 봇 명의로 올라간다.** 다른 채널과 의미가 다르므로 화면에서 배포 단위를 따로 설명해야 한다.

## 9. API

- **`POST /posts`:** 조각 목록과 공개 범위를 받아 게시글을 발행한다. 요청 본문을 받는 첫 경로이므로 검증 규약을 여기서 정한다.
- **배포 요청:** 게시글 발행 요청에 선택된 채널을 함께 받아 배포 행을 만든다. 별도 경로로 나누지 않는다.
- **`GET /connections`:** 내 연동 목록과 각 채널의 상태를 반환한다. 토큰은 반환하지 않는다.
- **연동 시작과 복귀:** 채널별 인가 주소를 발급하고 복귀 지점에서 토큰을 교환한다. 복귀 경로 파라미터는 내부 경로만 허용한다.
- **배포 상태 구독:** 게시글의 채널별 상태를 서버 전송 이벤트로 보낸다. 연결이 끊기면 조회로 대체할 수 있어야 한다.
- **재시도:** 특정 배포를 다시 시도한다. 부분 게시는 남은 지점부터 이어 붙인다.

## 10. 구현 단계

| 단계             | 내용                                                        | 확인 기준                                              |
| ---------------- | ----------------------------------------------------------- | ------------------------------------------------------ |
| 1. 콘텐츠        | `posts`·`post_segments` 마이그레이션, `POST /posts`, 검증 규약 | 조각 여럿을 가진 게시글이 발행되고 피드에 이어져 보인다 |
| 2. 작성 화면     | 조각 추가·삭제·순서, 임시 저장                              | 작성 흐름이 데이터와 연결된다                          |
| 3. 연동 기반     | `social_connections`, 토큰 보관, 팝업 연동 흐름             | 연동과 해제가 되고 작성 중인 내용이 유지된다           |
| 4. 배포 기반     | `distributions`·`distribution_items`, 워커, 상태 전달       | 채널 하나로 연속 게시가 끝까지 성공한다                |
| 5. Bluesky       | 링크 범위 계산, 연속 게시, 재시도                           | 부분 실패 후 이어서 재시도가 된다                      |
| 6. 화면 완성     | 채널 선택, 한도 표현, 배포 결과 요약, 진행 화면             | 발행 전 고지와 실제 결과가 일치한다                    |
| 7. Threads       | 두 단계 발행, 심사 제출                                     | 심사 통과 후 일반 계정에 발행된다                      |
| 8. LinkedIn      | 조각 합치기, 재연동 안내                                    | 만료된 연동이 재연동으로 복구된다                      |
| 9. X             | 연동 해제 상태 제거, 비용 정책 반영                         | 비용 결정 이후에 착수한다                              |

## 11. 남은 결정

- 외부 배포에 Rillroot 원문 주소를 함께 붙일지 여부
- X의 건당 비용을 서비스가 부담할지, 창작자의 키를 쓰게 할지
- 발행 후 수정과 삭제를 외부 채널에 반영할지 여부
- 예약 발행 도입 여부와 채널 약관상의 허용 범위
- 미디어 첨부의 지원 범위와 채널별 변환 규칙
- 조각을 나누지 않고 긴 글을 쓴 경우의 보조 분할 도입 여부
