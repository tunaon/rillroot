# Rillroot 인증 설계

| 항목        | 내용                                                 |
| ----------- | ---------------------------------------------------- |
| 문서        | 인증·회원 로그인 설계                                |
| 상태        | 설계 확정, 구현 전                                   |
| 기준 문서   | Rillroot PRD v0.2                                    |
| 인증 서비스 | Supabase Auth                                        |
| 로그인 수단 | Google, 이메일 인증코드                              |
| 서버 경계   | 웹은 Supabase Auth로 로그인하고, DB는 API만 접근한다 |

## 1. 범위

이 문서는 로그인과 가입 흐름, 세션 처리, 프로필 행 생성, handle·닉네임 자동 생성, 서버의 권한 판단, 개발과 메일 발송 환경을 정의한다.

프로필 페이지, 프로필 편집, 탈퇴 처리, 저장·팔로우 기능은 이 문서의 범위 밖이며 해당 기능을 설계할 때 정의한다.

## 2. 결정 요약

| 항목             | 결정                                                        |
| ---------------- | ----------------------------------------------------------- |
| 인증 서비스      | Supabase Auth                                               |
| 로그인 수단      | Google, 이메일 6자리 인증코드                               |
| 계정 연결        | 같은 이메일이면 두 수단이 한 계정으로 연결된다              |
| 로그인 진입      | 상단바 로그인 버튼, 회원 행동 시도                          |
| 온보딩           | 두지 않는다                                                 |
| 아바타           | 아이덴티콘을 기본으로 하고, 사진을 올리면 교체한다          |
| handle·닉네임    | 가입 시 자동 생성하고 같은 값으로 시작한다                  |
| handle 생성 위치 | API                                                         |
| DB 접근          | API만 접근하고 권한 판단도 API가 한다                       |
| RLS              | 켜고 정책은 두지 않는다                                     |
| 로그인 후        | 원래 페이지로 복귀하고, 하려던 행동은 자동 실행하지 않는다  |
| 로그인 문구      | 웹과 메일 모두 sign in으로 통일한다                         |
| 개발 환경        | 출시 전까지 원격 프로젝트 하나를 개발과 운영에 함께 쓴다    |
| 메일 발송        | Resend 무료로 시작하고 한도에 가까워지면 AWS SES로 전환한다 |
| API 키           | publishable·secret 키를 쓴다                                |

## 3. 아키텍처

```
브라우저 ──로그인──▶ Supabase Auth ──▶ 세션을 쿠키에 저장
  │
  ├─ Next (apps/web)
  │    proxy         요청마다 세션 갱신, 로그인 여부 판단
  │    /api/auth/callback  Google 로그인 복귀 지점
  │
  └─ Authorization: Bearer ──▶ NestJS (apps/api)
                                 ├ 토큰 검증
                                 ├ 권한 판단
                                 └ DB 접근 (secret key) ──▶ Postgres
                                      ├ auth.users      Supabase가 관리한다
                                      └ public.profiles 이 서비스가 관리한다
```

1. **자격 증명 검증과 계정 생성은 Supabase Auth가 맡는다.** 이 서비스는 가입·로그인 API를 만들지 않는다. Next 서버는 콜백에서 세션 쿠키를 저장하고 요청마다 세션을 갱신하는 역할만 하며, API 서버는 로그인 처리에 관여하지 않는다.
2. **웹은 API를 호출할 때 access token을 Bearer로 보낸다.**
3. **API는 토큰 서명을 공개키로 검증한다.** 요청마다 인증 서버에 묻지 않는다.
4. **DB는 API만 접근한다.** secret key가 RLS를 우회하므로 권한 판단은 API 코드가 책임진다.
5. **Bearer 방식을 유지한다.** 이후 모바일 앱도 같은 API를 쓴다.

## 4. 로그인 흐름

### 4.1 진입점

비회원 상태에서 상단바의 로그인 버튼을 누르거나, 작성·저장·팔로우 등 회원 행동을 시도하면 로그인을 연다. 로그인이 끝나면 원래 페이지로 돌아간다. 하려던 행동은 자동으로 실행하지 않는다.

### 4.2 이메일 인증코드

1. 이메일을 입력하면 그 주소로 6자리 코드를 발송한다.
2. 코드를 검증한다. 처음 인증한 사용자는 가입과 동시에 로그인되고, 기존 회원은 로그인된다.
3. 세션이 쿠키에 저장된다.
4. 웹이 내 프로필을 요청한다. 프로필이 없으면 이때 생성된다.

코드 재발송은 정해진 간격 이후에만 허용한다. 코드 만료 시간은 기본값보다 짧게 설정한다.

### 4.3 Google

1. Google에서 인증하면 Supabase를 거쳐 `/api/auth/callback`으로 돌아온다.
2. 콜백에서 세션을 쿠키에 저장하고 원래 경로로 이동한다.
3. 이후 프로필 요청 과정은 이메일 흐름과 같다.

### 4.4 세션

세션은 무기한 유지한다. access token은 짧은 수명을 가지며 만료 전에 자동으로 교체된다. 토큰 교체는 프로필 요청을 일으키지 않는다.

## 5. 데이터 모델

### 5.1 profiles

| 컬럼                      | 규칙                                                    |
| ------------------------- | ------------------------------------------------------- |
| `id`                      | 인증 사용자 id를 참조하고, 사용자 삭제 시 함께 삭제된다 |
| `handle`                  | 소문자로만 저장하고 유일하다                            |
| `display_name`            | 비워 두지 않으며, 길이 상한만 두고 중복을 허용한다      |
| `avatar_url`              | 비어 있으면 아이덴티콘을 표시한다                       |
| `created_at`·`updated_at` | 생성·수정 시각                                          |
| `last_country`            | 마지막 접속 국가, 두 글자 대문자                        |
| `last_provider`           | 마지막 로그인에 쓴 수단                                 |
| `last_signed_in_at`       | 마지막으로 반영한 로그인 시각                           |

- **인증 스키마에 있는 값은 복사하지 않는다.** 이메일과 연결된 인증 수단 목록이 여기에 해당한다. 복사본은 어긋나고 노출 범위만 넓힌다.
- **접속 기록은 인증 스키마에 남지 않으므로 여기에 둔다.** 접속 국가는 어디에도 저장되지 않고, 로그인 수단은 로그아웃과 함께 지워진다.
- **접속 기록은 응답에 담지 않는다.** 통계용이며 화면에서 쓰지 않는다.
- **컬럼은 필요한 기능과 함께 추가한다.**

### 5.2 접근 제어

- **테이블을 만드는 마이그레이션에서 RLS를 켠다.**
- **정책은 두지 않는다.** 정책이 없다는 것은 직접 접근을 전부 차단한다는 뜻이며, 공개된 publishable key로 들어오는 요청을 막기 위한 의도적 선택이다.
- **공개 조회를 포함한 모든 접근은 API를 거친다.**
- **스키마는 마이그레이션 파일로만 변경한다.** 대시보드에서 직접 수정하지 않는다.

## 6. handle과 닉네임

### 6.1 자동 생성

- 가입 시 자동으로 생성하고, 닉네임은 같은 값으로 시작한다.
- 영어 소문자 형용사와 명사를 이어 붙이고 숫자 두 자리를 더한다. 각 단어는 다섯 자 이하로 제한해 결과가 열네 자를 넘지 않게 한다.
- 단어 목록은 부정적이거나 공격적인 표현을 제외해 직접 관리한다.
- 이미 있는 값이면 다시 생성한다. 연속으로 충돌하면 숫자 자리를 늘린다.

### 6.2 저장 규칙

- 길이는 세 자에서 열다섯 자까지 허용하고, 영어 소문자와 숫자와 밑줄만 쓴다.
- 소문자로만 저장하므로 유일 제약 하나로 중복을 막는다.
- 운영자를 사칭하는 단어와 서비스 경로에 쓰이는 단어는 예약어로 막는다.
- 닉네임은 중복을 허용하고 길이 상한만 둔다.
- 이 규칙은 공용 패키지에 두고 웹 입력 검증과 API가 함께 쓴다.

## 7. 아바타

아바타 기본값은 아이덴티콘이며 씨앗값은 프로필 id다. 사용자가 사진을 올리면 그 값으로 교체한다. 외부 계정의 사진은 가져오지 않는다.

## 8. API

- **인증 가드:** Bearer 토큰을 공개키로 검증하고 요청에 사용자 id를 붙인다. 검증에 실패하면 401을 반환한다.
- **`GET /profiles/me`:** 내 프로필을 반환하고, 없으면 생성한다. 프로필 생성은 이 경로에만 둔다. 접속 기록도 이 경로에서 남긴다.
- **접속 국가:** 웹이 전달한 값이 저장된 값과 다를 때만 갱신한다. 두 글자 국가 코드가 아니면 기록하지 않는다.
- **로그인 수단:** 토큰의 로그인 시각이 저장된 값보다 새로울 때만 정한다. 소셜 로그인은 토큰에 서비스 이름이 없으므로, 인증 서버의 identity 중 마지막으로 쓰인 것에서 이름을 얻는다. 수단을 정하지 못하면 시각도 남기지 않아 다음 요청에서 다시 시도한다.
- **공개 프로필 조회:** 별도 경로로 추가하며 공개 컬럼만 선택해 반환한다.
- **민감한 작업:** 차단이나 탈퇴 직후의 토큰이 남아 있을 수 있으므로 사용자 상태를 인증 서버에 직접 확인한다.

## 9. 웹

- **클라이언트 구성:** 브라우저용, 서버 컴포넌트용, proxy용 클라이언트를 나눈다.
- **proxy:** 세션 갱신과 다국어 미들웨어를 하나로 합친다.
- **`/api/auth/callback`:** 언어 경로 밖에 두어 복귀 주소를 하나로 유지하고 proxy 대상에서 제외한다. 복귀 경로 파라미터는 내부 경로만 허용한다.
- **로그인 모달:** 첫 단계에서 Google 버튼과 이메일 입력을 보여 주고, 다음 단계에서 코드 입력과 재발송과 이메일 변경을 제공한다. PC와 모바일은 같은 구성 요소를 형태만 달리해 쓴다.
- **오류 메시지:** 인증 서비스의 오류 코드를 지원 언어 메시지로 매핑한다.
- **상단바:** 로그인 여부를 서버의 첫 렌더에서 판단해 화면이 바뀌지 않게 한다.
- **프로필 요청 횟수:** 레이아웃에서 한 번 요청한다. 사이트 안에서 페이지를 이동할 때는 다시 요청하지 않는다.
- **접속 국가 전달:** 프로필을 요청할 때 요청 헤더의 국가 코드를 함께 보낸다. 사용자가 바꿀 수 있는 값이므로 통계 외의 판단에는 쓰지 않는다.
- **로그인 직후:** 캐시에 남은 비회원 화면을 다시 그린다.

## 10. 보안 원칙

- **secret key는 서버에만 둔다.** 클라이언트로 노출되는 접두사를 붙이지 않으며 값을 저장소에 올리지 않는다.
- **publishable key는 공개를 전제로 한다.** 이 키로 들어오는 직접 접근은 RLS가 막는다.
- **세션 쿠키는 HttpOnly가 아니다.** 브라우저가 토큰을 갱신해야 하므로, 스크립트 삽입 공격은 CSP로 막는다.
- **복귀 경로는 내부 경로만 허용한다.** 외부 주소로 보내는 공격을 막는다.
- **공개 응답은 공개 컬럼만 선택한다.** 전체 컬럼을 그대로 반환하지 않는다.
- **자동화된 발송 유발에 대비한다.** CAPTCHA와 발송 제한을 함께 쓴다.

## 11. 환경과 메일 발송

### 11.1 환경

- 출시 전까지 원격 프로젝트 하나를 개발과 운영에 함께 쓴다.
- 실제 사용자 데이터가 생기기 전에 개발 환경을 분리한다.
- 인증 메일은 영어로 발송한다.

### 11.2 발송 경로

- **기본 발송 서버는 운영에 쓰지 않는다.** 수신 주소와 시간당 발송량에 제한이 있어 확인용으로만 쓸 수 있다.
- **초기 발송은 Resend 무료 플랜을 커스텀 SMTP로 연결해 쓴다.** 선택 기준은 상시 무료 여부, 인증 메일에 발송 서비스 표시가 붙지 않을 것, 발송 기록 보관 기간이다.
- **인증 메일은 루트 도메인에서 보낸다.** 마케팅 메일을 도입할 때 그 발송에 별도 서브도메인을 두어 분리하고, 링크 추적은 그쪽에만 켠다.
- **전환 기준:** 하루 발송량이 한도의 일정 비율에 닿으면 AWS SES로 옮길 준비를 시작하고, 그 시점에 단가와 조건을 다시 확인한다. 도메인 인증과 심사에 시간이 걸리므로 한도에 부딪힌 뒤에 시작하지 않는다.
- **전환 방법:** SMTP 설정 값 교체로 끝난다. 코드와 메일 템플릿은 그대로 둔다. 이전 발송 서비스 계정은 예비 경로로 남긴다.

## 12. 구현 단계

| 단계         | 내용                                                                                        | 확인 기준                                                       |
| ------------ | ------------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| 0. 준비      | 도메인, 발송 서비스 연결, Google 인증 등록, 복귀 주소와 메일 템플릿 설정, CLI 초기화와 연결 | 인증 메일 수신, API가 새 키로 DB 연결                           |
| 1. DB        | profiles 마이그레이션과 RLS, 타입 생성                                                      | 공개 키로 직접 조회가 차단된다                                  |
| 2. API       | 인증 가드, `GET /profiles/me`, handle 생성                                                  | 토큰 없으면 401, 첫 호출에 프로필 생성, 충돌 재시도 테스트 통과 |
| 3. 웹 기반   | 클라이언트, proxy 병합, `/api/auth/callback`                                                | 새로고침 후 로그인 유지, 다국어 경로 정상, 토큰 자동 교체       |
| 4. 웹 화면   | 로그인 모달, 상단바, 행동 가드, 로그아웃, 오류 메시지                                       | 두 수단과 신규·기존 조합의 네 경로 통과, 원래 페이지 복귀       |
| 5. 배포 준비 | 커스텀 도메인, 유료 플랜 검토                                                               | 운영 도메인에서 메일 수신과 인증 화면 확인                      |

## 13. 남은 결정

- handle 변경 정책과 변경 주기
- 글 URL에 handle을 포함하지 않는 원칙의 확정
- 탈퇴 시 글 처리 방식
- 커스텀 도메인 적용 시점
- 개발 환경 분리 방식과 시점
